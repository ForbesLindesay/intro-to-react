const React = require('react');
const ReactDOM = require('react-dom');
const WelcomeForm = require('./WelcomeForm');

const root = document.getElementById('root');

ReactDOM.render(<WelcomeForm />, root);
